[package]
name = "yeokja-assemble"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
yeokja-core = { path = "../core" }
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
