; Only index TeX Live fonts and $OSFONTDIR (the Nanum fonts from the nix
; devShell). Scanning the OS font directories is not needed and can take hours
; on machines with very large font collections.
[db]
    location-precedence = texmf
